{
  "name": "metro-rush-3d",
  "version": "2.1.0",
  "private": true,
  "type": "module",
  "description": "An original 3D endless subway runner with keyboard and touch controls",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check dist/game.js && node --check dist/engine.js && node --check dist/renderer.js"
  },
  "engines": { "node": ">=20" }
}
